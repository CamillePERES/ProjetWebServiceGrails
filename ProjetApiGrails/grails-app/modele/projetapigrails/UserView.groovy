package projetapigrails

class UserView {

    /***
     * Ici, je recree un user qui a pour attribut :
     * un user (username, password)
     * une liste de roles
     */
    User user;
    List<Role> roles;
}
